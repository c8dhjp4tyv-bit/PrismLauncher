name: Build Application and Make Release

on:
  push:
    tags:
      - "*"

permissions: {}

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      # Required for Azure Trusted Signing
      id-token: write
      # Required for vcpkg binary cache
      packages: write
    with:
      build-type: Release
      environment: Release
    secrets: inherit

  create_release:
    needs: build_release
    permissions:
      contents: write
    runs-on: ubuntu-slim
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: "true"
          path: "TimelessLauncher-source"
      - name: Download artifacts
        uses: actions/download-artifact@v8
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/TimelessLauncher-source TimelessLauncher-${{ env.VERSION }}
          mv TimelessLauncher-Linux-Qt6-Portable*/TimelessLauncher-portable.tar.gz TimelessLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv TimelessLauncher-Linux-aarch64-Qt6-Portable*/TimelessLauncher-portable.tar.gz TimelessLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv TimelessLauncher-*.AppImage/TimelessLauncher-*-x86_64.AppImage TimelessLauncher-Linux-x86_64.AppImage
          mv TimelessLauncher-*.AppImage.zsync/TimelessLauncher-*-x86_64.AppImage.zsync TimelessLauncher-Linux-x86_64.AppImage.zsync
          mv TimelessLauncher-*.AppImage/TimelessLauncher-*-aarch64.AppImage TimelessLauncher-Linux-aarch64.AppImage
          mv TimelessLauncher-*.AppImage.zsync/TimelessLauncher-*-aarch64.AppImage.zsync TimelessLauncher-Linux-aarch64.AppImage.zsync
          mv TimelessLauncher-macOS*/TimelessLauncher.zip TimelessLauncher-macOS-${{ env.VERSION }}.zip
          mv TimelessLauncher-macOS*/TimelessLauncher.dmg TimelessLauncher-macOS-${{ env.VERSION }}.dmg

          tar --exclude='.git' -czf TimelessLauncher-${{ env.VERSION }}.tar.gz TimelessLauncher-${{ env.VERSION }}

          for d in TimelessLauncher-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="TimelessLauncher-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv TimelessLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in TimelessLauncher-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="TimelessLauncher-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv TimelessLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: Timeless Launcher ${{ env.VERSION }}
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            TimelessLauncher-Linux-x86_64.AppImage
            TimelessLauncher-Linux-x86_64.AppImage.zsync
            TimelessLauncher-Linux-aarch64.AppImage
            TimelessLauncher-Linux-aarch64.AppImage.zsync
            TimelessLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            TimelessLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
            TimelessLauncher-Windows-MinGW-w64-${{ env.VERSION }}.zip
            TimelessLauncher-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            TimelessLauncher-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            TimelessLauncher-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            TimelessLauncher-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            TimelessLauncher-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            TimelessLauncher-Windows-MSVC-${{ env.VERSION }}.zip
            TimelessLauncher-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            TimelessLauncher-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            TimelessLauncher-macOS-${{ env.VERSION }}.zip
            TimelessLauncher-macOS-${{ env.VERSION }}.dmg
            TimelessLauncher-${{ env.VERSION }}.tar.gz
